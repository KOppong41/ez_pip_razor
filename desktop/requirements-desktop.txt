# Reuse the main project requirements plus desktop-only helpers.
-r ../requirements.txt

# Local Windows API server and packager
waitress==3.0.2
pyinstaller==6.22.2
pyyaml==6.0.1
pywin32==312; sys_platform == "win32"
# Setuptools runtime helpers used by PyInstaller hooks.
jaraco.functools==4.6.0
jaraco.context==6.1.2
jaraco.text==4.3.0
